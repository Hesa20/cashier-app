# Frontend example environment variables (placeholder-only)
# Copy to `.env.local` and fill real values for development. Do NOT commit secrets.

# If using local proxy to backend (Next.js rewrites) set to '/api' or to the full API URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# Supabase public values (safe for front-end only: anon key)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Note: DO NOT put SUPABASE_SERVICE_ROLE_KEY or DATABASE_URL here. Backend-only secrets must be stored
# in backend env files or secrets manager (e.g., apps/api/.env, GitHub Actions secrets, Netlify/Vercel env).
