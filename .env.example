# Backend API Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000

## Frontend (public)
# If you use Supabase from the client, set the public values below
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

## Backend (server-side) - do NOT commit service_role keys
# These should be set in server env (apps/api/.env or CI secrets)
SUPABASE_URL=
SUPABASE_KEY=
DATABASE_URL=
