# Backend (apps/api) environment example

# Server
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# Database (Postgres) - use a full connection string in production
# Example: postgresql://username:password@host:5432/dbname
DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:5432/<DB_NAME>

# If using Supabase for DB/functions:
SUPABASE_URL=https://your-project.supabase.co
# Use service role key ONLY on backend/CI (never expose to frontend)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional - anon key (only if backend needs to call public supabase endpoints)
SUPABASE_ANON_KEY=your-anon-key

# Security / Auth
JWT_SECRET=your_jwt_secret_here

# Frontend URL used for CORS allowed origins (production)
FRONTEND_URL=http://localhost:3000
