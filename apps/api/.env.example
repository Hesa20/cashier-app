# Backend example environment variables (placeholder-only)
# Copy this file to `apps/api/.env` and fill real values for local development.

PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# Supabase (fill real values in apps/api/.env - DO NOT commit secrets)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key    # backend-only: never expose to frontend or commit

# Database (Postgres) - backend only
DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<db_name>

# Frontend URL used for CORS (production)
FRONTEND_URL=http://localhost:3000